server:
    port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8099/eureka/

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: client-service
          uri: lb://client-service
          predicates:
            - Path=/api/client/**
        - id: income-product-service
          uri: lb://income-product-service
          predicates:
            - Path=/api/incomes/**
        - id: outcome-product-service
          uri: lb://outcome-product-service
          predicates:
            - Path=/api/outcomes/**
        - id: transaction-service
          uri: lb://transaction-service
          predicates:
            - Path=/api/transaction/**
        - id: transfer-service
          uri: lb://transfer-service
          predicates:
            - Path=/api/transfer/**